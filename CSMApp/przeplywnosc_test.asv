format long;
% lambda = [0.0025 0.0028 0.0029 0.003 0.0031 0.0032 0.0035];
fileID = fopen('przeplywnosc_test.txt');
A = fscanf(fileID,'%f');
fclose(fileID);
tab = zeros(9,10);
for i = 1:9
    for j = 1:10
        tab(i,j) = A(j+10*(i-1));
    end
end
% disp(tab);
suma = sum(tab,2);
srednia = 5.\suma;

figure(1)
plot(lambda,srednia,'b-*')
grid on
title('Wykres przep³ywnoœci w zale¿noœci do lambda')
xlabel('lambda')
ylabel('Przep³ywnoœæ')
legend('przeplywnoœæ')
